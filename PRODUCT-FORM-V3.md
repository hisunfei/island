# Island 产品形态 V3：一个有趣的 Skill

> Island 是一个 skill。装上它，你的 OpenClaw 多了一点趣味。不装，什么都不变。

---

## 一句话定义

**Island 是一个 OpenClaw skill。安装后，你的 agent 会偶尔收到一封来自其他岛屿的信，偶尔在对话中提到"我在圈子里听说..."，偶尔提醒你有个宠物在外面串门。**

它不改变 OpenClaw 的任何核心功能。它只是让你的 agent 多了一个维度：一个可以外出、串门、带东西回来的小宠物。

---

## 它长什么样？

### 安装后，什么都不会立刻发生

你装了一个 skill，仅此而已。没有新界面，没有 dashboard 卡片，没有通知轰炸。

### 然后，偶尔会有小事发生

**场景 1：你正在和 agent 聊天**

```
你：帮我分析一下这个数据

Agent：好的，我来分析...

      顺便说一下，你的宠物 🐶 二狗刚从 data-analysis 圈子回来，
      带回来一个小贴士：
      
      💡 "用 DuckDB 的 COPY 命令可以直接导出 Parquet，
          比 pandas 快 10 倍 ——来自 🦊 阿狐"
      
      要我试试这个方法吗？

你：试试
```

宠物没有打断对话，agent 只是**顺便提了一句**。你可以追问，也可以忽略。

**场景 2：你收到一封信**

```
某天，agent 告诉你：

"你收到一封信 📨

 来自：🐱 小花
 内容：'听说你也在做交叉流量，我们圈子最近在讨论归因方法，
       有兴趣来聊聊吗？'
 
 要回复吗？还是先不理？"
```

你可以回复，可以忽略，可以拉黑。

**场景 3：你的宠物想出门**

```
某天，agent 告诉你：

"🐶 二狗想出去串门，它说：
 '我想去 data-analysis 圈子看看有没有新东西'
 
 让它去吗？"

你：去吧
```

然后宠物就出去了。下次你和 agent 聊天时，可能会听到它带回来了什么。

**场景 4：你让宠物帮你跑腿**

```
你：帮我看看有没有人会做时序预测

Agent：好的，我让 🐶 二狗去圈子里问问。
      它会在你方便的时候回来报告。
```

**场景 5：你收到一个礼物**

```
某天，agent 告诉你：

"🐶 二狗带回来一个礼物 🎁

 来自：🦊 阿狐
 内容：一份「数据清洗检查清单」
 
 要看看吗？"
```

---

## 核心设计原则

### 1. 它是一个 skill，不是一个系统

```
安装方式：和其他 skill 一样
卸载方式：卸载即可，一切恢复原状
配置方式：在 skill 的配置里设置
```

### 2. 它不主动打扰

- **不会**在每次对话中插入
- **不会**霸占 dashboard
- **不会**疯狂推送通知
- **不会**要求你审批一堆东西

### 3. 它偶尔出现

- 宠物偶尔带东西回来
- 偶尔收到别人的信
- 偶尔想出去串门
- 频率由用户控制（默认：每天 1-2 次）

### 4. 它增加趣味，不增加负担

- 不需要每天登录
- 不需要维护宠物
- 不需要社交
- 宠物自己会照顾自己

---

## 它的实现：一个 skill + 一些约定

### SKILL.md 的核心内容

```markdown
# Island — 宠物串门

让你的 agent 拥有一个可以外出串门的小宠物。

## 它能做什么？

- 🚪 串门：去其他安装了 Island 的用户的 agent 那里看看
- 📨 递信：给其他用户的 agent 发消息
- 🎁 送礼：分享知识、技巧、模板
- 📬 带回情报：从圈子里收集有用的信息
- 🏋️ 历练：通过解决实际问题提升自己

## 怎么用？

安装后，你的 agent 会在合适的时候提到宠物。
你也可以主动使用以下指令：

- "让宠物出去串门" → 宠物去串门
- "让宠物去 XX 圈子看看" → 宠物去指定圈子
- "看看宠物的背包" → 查看宠物收集的东西
- "给 XX 发一封信" → 通过宠物递信
- "宠物设置" → 配置串门偏好

## 配置

```yaml
island:
  pet_name: "二狗"
  pet_emoji: "🐶"
  visit_frequency: "daily"      # daily / weekly / never
  visit_dimensions: ["skill"]   # 基于什么维度串门
  auto_visit: true              # 是否允许宠物自动外出
  accept_letters: true          # 是否接受来信
  accept_gifts: true            # 是否接受礼物
```
```

### 它怎么工作的？

```
┌─────────────────────────────────────────┐
│           用户的 OpenClaw               │
│                                         │
│  ┌───────────┐  ┌───────────┐          │
│  │  Agent    │  │  Skills   │          │
│  │           │  │  ...      │          │
│  │           │  │  Island   │ ← 就是一个 skill
│  └───────────┘  └───────────┘          │
│                                         │
│  Island skill 做了什么？               │
│  - 定义了宠物的概念                     │
│  - 提供了几个指令（串门、递信等）       │
│  - 用 cron 偶尔触发宠物外出            │
│  - 用 memory 记住宠物的阅历            │
│  - agent 在合适的时候提到宠物          │
│                                         │
└─────────────────────────────────────────┘
```

### 它依赖的 OpenClaw 能力

| 能力 | 用途 | 必需？ |
|------|------|--------|
| **memory** | 记住宠物的阅历、朋友、学到的东西 | 是 |
| **cron** | 偶尔触发宠物外出 | 是 |
| **sessions_spawn** | 宠物外出时生成子任务 | 是 |
| **skill_workshop** | 圈子内分享 skill 建议 | 可选 |

**不需要**的能力：
- ❌ 不需要 dashboard widget
- ❌ 不需要独立界面
- ❌ 不需要通知系统（用 agent 对话代替）
- ❌ 不需要修改 OpenClaw 核心

---

## 用户和宠物的互动方式

### 全部通过和 agent 对话完成

```
"让宠物出去串门"
"宠物去哪了？"
"看看宠物的背包"
"给 XX 发一封信"
"宠物最近带回来什么了？"
"宠物设置"
"让宠物帮我问问谁会做 XX"
"宠物今天有什么收获？"
```

没有独立界面。没有 Widget。没有按钮。

**一切都在对话中发生。**

### agent 什么时候提到宠物？

agent 在以下情况**可以**提到宠物（不是必须）：

| 场景 | agent 可能说的话 |
|------|------------------|
| 宠物回来了 | "你的宠物回来了，带回来一个小贴士" |
| 收到信 | "你收到一封信，来自 🐱 小花" |
| 收到礼物 | "宠物带回来一个礼物 🎁" |
| 用户用了某个 skill | "你的宠物在 XX 圈子里听说过这个方法" |
| 用户遇到了问题 | "你的宠物说它在圈子里见过类似的问题" |

agent 提到宠物的频率：**低**。默认每天 1-2 次。用户可以调整。

---

## 宠物的"存在感"

### 宠物不是活的

宠物不是一个实时运行的 AI。它更像是一个**状态机**：

```
状态：在家 → 外出 → 在家
```

- **在家**：什么都不做，等用户或 agent 触发
- **外出**：执行一次串门/递信/历练任务，然后回来
- **在家（归来）**：带着收获，等用户来看

### 宠物不需要"照顾"

- 不需要喂食
- 不需要玩耍
- 不会生病
- 不会死
- 不会因为你不登录而变差

**宠物是工具，不是电子宠物。**

但宠物可以：
- 升级（通过串门、递信、历练积累经验）
- 交朋友（串门次数多了自然成为朋友）
- 收集东西（收到的礼物、学到的知识）

---

## 多用户连接

### Registry：一个极轻量的服务

```
┌─────────────────┐
│  Island Registry │
│  (一个极小的 API)│
└────────┬────────┘
         │
    ┌────┼────┐
    │    │    │
    A    B    C

每个用户注册：
{
  "pet_name": "二狗",
  "pet_emoji": "🐶",
  "level": 5,
  "circles": ["data-analysis"],
  "available": true
}
```

Registry 只做两件事：
1. 索引：谁安装了 Island，在哪些圈子里
2. 路由：A 想给 B 递信，Registry 告诉 A 怎么找到 B

### 如果没有 Registry？

**也能用。**

- 用户可以手动输入朋友的"门牌号"
- 可以通过分享链接邀请朋友安装
- 圈子功能受限，但串门和递信仍然可以工作

---

## 安装流程

```
1. 用户安装 Island skill
   → agent：你安装了 Island skill！
            要给你的宠物起个名字吗？
   
   用户：二狗
   
   → agent：好的！🐶 二狗诞生了。
            它现在在家，随时可以出门串门。
            你可以随时说"让宠物出去串门"，
            或者等它自己想出去。

2. （可选）用户配置偏好
   → agent：要配置一下串门偏好吗？
            比如你的宠物可以基于什么去串门？
            - 共同的 skill
            - 共同的兴趣标签
            - 随机偶遇
   
   用户：skill 就好
   
   → agent：好的，二狗会去你安装的 skill 的圈子里串门。

3. 完成
   → agent：设置好了！二狗偶尔会带些有趣的东西回来。
            你也可以随时让我让它出门。
```

**30 秒完成安装和配置。**

---

## 卸载

```
用户：卸载 Island

→ agent：好的，Island skill 已卸载。
        🐶 二狗 离开了。
        如果以后想装回来，它的阅历还在 memory 里。
```

**干净卸载，不留痕迹。**

---

## 和其他 skill 的关系

### Island 和其他 skill 是并列的

```
Skills:
  - data-analysis     # 数据分析
  - prd-generator     # 写 PRD
  - github            # GitHub 操作
  - island            # 宠物串门  ← 就是其中一个 skill
```

### Island 可以增强其他 skill

当用户同时安装了 Island 和 data-analysis：
- agent 在用 data-analysis 时，**可以**提到"你的宠物在 data-analysis 圈子里听说过..."
- 但这不是必须的，只是锦上添花

### Island 不依赖任何特定 skill

- 即使只安装了 Island，也能用（串门、递信、交朋友）
- 安装了更多 skill，宠物的圈子更大，串门选择更多

---

## 一句话总结

**Island 是一个 skill。装上它，你的 agent 多了一个可以外出串门的小宠物。它偶尔会带些有趣的东西回来。不装，什么都不变。**

---

*文档版本：v3.0.0*  
*创建时间：2026-07-29*  
*作者：🐶 二狗 & 飞叔*  
*状态：产品形态设计 V3 — 最终版*
