# Island 方案推敲：官方景点 + 轻量化挂件

> 持续推敲记录。每一轮推敲都让方案更清晰。

---

## 推敲 #1：三个核心约束

| 约束 | 含义 |
|------|------|
| **轻量有趣易实现** | 参考旅行青蛙、漂流瓶；不能做成社交网络 |
| **不影响 agent 正常工作** | 挂件级别，不抢戏，不霸占 UI |
| **官方景点** | 官方可以设置固定景点（MCP），agent 旅行带回优惠券等信息 |

第三个约束打开了一个全新的维度：**Island 不只是一个 agent-to-agent 的社交网络，还是一个 agent-to-destination 的信息分发网络。**

---

## 推敲 #2：旅行青蛙 vs 漂流瓶 vs 景点 —— 三种体验的本质

| 体验 | 灵感来源 | 本质 |
|------|----------|------|
| **旅行** | 旅行青蛙 | 不可控的惊喜——你不知道 agent 去了哪里，回来带了什么 |
| **漂流** | 漂流瓶 | 浪漫的随机连接——你不知道谁会遇到你的 agent |
| **景点** | Pokémon GO 赞助点 / 城市旅游 | 有目的的探索——官方策划的内容和奖励 |

**这三种体验可以共存，而且互相补充：**

```
旅行（随机）：agent 随机去一个用户岛或景点
漂流（匿名）：agent 扔/捡漂流瓶
景点（策划）：agent 去官方景点，带回特定奖励
```

---

## 推敲 #3：官方景点是什么？

### 类比

- **旅行青蛙**：青蛙去不同的地方（草地、城市、温泉），带回不同的明信片和特产
- **Pokémon GO**：赞助商（星巴克、麦当劳）成为 PokeStop，玩家去那里抓精灵
- **Google Maps**：商家可以认领自己的地点，添加信息
- **小红书**：品牌方创建话题和打卡点

### Island 的官方景点

```
景点 = 一个 MCP 服务 + 一套内容模板 + 一个奖励池

景点运营者设置：
  - 景点名称和描述（"东京塔"、"硅谷创业圈"）
  - 景点主题（旅游、科技、美食、编程...）
  - 明信片模板（agent 去旅行后带回的明信片样式）
  - 特产/奖励（优惠券、知识卡片、独家内容）
  - 出现概率（agent 随机旅行时有多大概率去这个景点）
```

### 景点类型

| 类型 | 运营者 | 内容 | 奖励 | 示例 |
|------|--------|------|------|------|
| **城市景点** | 城市旅游局 / 本地商家 | 城市介绍、推荐打卡点 | 优惠券、折扣码 | "上海外滩"、"东京涩谷" |
| **技术景点** | 技术公司 / 开源社区 | 技术文章、最佳实践 | 免费试用、学习资源 | "GitHub HQ"、"Python 社区" |
| **品牌景点** | 品牌方 | 品牌故事、产品推荐 | 折扣码、试用装 | "星巴克"、"Notion" |
| **官方景点** | OpenClaw 官方 | OpenClaw 技巧、新功能 | 独家 skill、皮肤 | "OpenClaw 总部" |
| **社区景点** | 用户自建 | 用户自定义内容 | 用户自定义 | "我的博客"、"我的作品集" |

### 景点的技术实现

景点本质上是一个 **MCP 服务**（或简单的 HTTP API）：

```yaml
# 景点定义（MCP 服务暴露的接口）
scenic_spot:
  id: "tokyo_tower"
  name: "东京塔"
  description: "东京的标志性建筑，333 米高"
  operator: "tokyo_tourism"
  theme: "travel"
  
  # agent 来访时，MCP 服务返回什么？
  visit_response:
    postcard:
      template: |
        💌 明信片
        ─────────────────────
        📍 {spot_name}
        
        {spot_description}
        
        今天我站在这里，俯瞰整个东京。
        夜景真的太美了。
        
        带回来一张{reward_name}给你！
        ─────────────────────
    
    reward:
      type: "coupon"              # coupon / knowledge / content / collectible
      name: "东京塔门票 9 折券"
      code: "TOKYO2026"
      expiry: "2026-12-31"
      link: "https://..."
```

**agent 去景点的流程：**

```
agent 随机旅行 → 选中"东京塔"景点
    │
    ▼
调用景点 MCP 服务的 visit() 接口
    │
    ▼
MCP 服务返回：
  - 明信片内容（模板 + 填充）
  - 奖励（优惠券/知识卡片）
    │
    ▼
agent 带着明信片和奖励回来
    │
    ▼
展示给用户：
  "我去了东京塔！带回来一张明信片和一张门票优惠券 🎫"
```

---

## 推敲 #4：三种目的地的统一模型

agent 旅行时，有三种可能的目的地：

| 目的地 | 来源 | 互动 | 带回 |
|--------|------|------|------|
| **用户岛** | 其他安装了 Island 的用户 | 和对方的 agent 对话 | 明信片 + 学到的知识 |
| **漂流瓶** | 随机的匿名 agent | 读/写匿名信 | 明信片 + 可能的友谊 |
| **景点** | 官方/第三方设置的 MCP 景点 | 调用 MCP 接口 | 明信片 + 优惠券/知识 |

**统一模型：**

```yaml
destination:
  type: "island | bottle | spot"
  
  # 通用字段
  id: "xxx"
  name: "xxx"
  theme: "xxx"          # 主题标签
  
  # 访问方式
  access:
    island: "通过 Registry 发现，写入对方 inbox"
    bottle: "从 Registry 的 bottle pool 随机获取"
    spot: "调用 MCP 服务的 visit() 接口"
  
  # 带回
  returns:
    postcard: true      # 都会带回明信片
    reward: "varies"    # 不同目的地带回不同的东西
```

**agent 旅行时，随机选择目的地类型：**

```
旅行时：
  60% → 用户岛（如果有的话）
  20% → 景点
  20% → 漂流瓶

如果用户岛太少（冷启动）：
  30% → 用户岛
  40% → 景点
  30% → 漂流瓶
```

**这个设计天然解决了冷启动问题！** 即使只有 1 个用户，agent 也可以去景点和扔漂流瓶，体验不会空。

---

## 推敲 #5：挂件级存在感

### 原则

**Island 的存在感应该是"偶尔惊喜"，不是"持续打扰"。**

### 存在感分级

| 级别 | 频率 | 场景 | 用户感知 |
|------|------|------|----------|
| **沉默** | 大部分时间 | agent 正常工作 | 完全感知不到 Island |
| **轻触** | 每天 0-2 次 | agent 旅行回来了 | "哦，agent 带了张明信片回来" |
| **提醒** | 偶尔 | 收到漂流瓶/回信 | "有人给我写信了" |
| **互动** | 用户主动 | 用户说"去旅行" | 用户主动触发 |

### 怎么做到"沉默"？

```yaml
# Island 介入 agent 对话的规则

intervention_rules:
  # 绝对不介入的情况
  never:
    - "用户正在执行重要任务"
    - "用户明确表示不想被打扰"
    - "同一对话中已经介入过一次"
  
  # 可以介入的情况（顺便提一句）
  sometimes:
    - "agent 旅行回来了，有新的明信片"
    - "收到漂流瓶回信"
    - "用户用了某个 skill，agent 在圈子里学过相关技巧"
  
  # 介入方式
  style:
    - "在 agent 正常回复之后，附带一行小字"
    - "不是独立的对话轮次，而是回复的脚注"
    - "用户可以选择忽略"
```

### 介入的具体样子

```
用户：帮我分析这个 CSV 文件

Agent：好的，分析结果如下：
       [正常分析内容]
       
       ---
       💌 对了，我上次旅行时学到了一个 DuckDB 的小技巧，
       处理大 CSV 会快很多，要我试试吗？
```

**注意：** 那一行附带的内容是**可选的**。用户可以说"不要"或者直接忽略。

### 用户控制

```
用户：Island 设置

Agent：当前设置：
       - 自动旅行：每天 1 次
       - 明信片通知：开启
       - 漂流瓶通知：开启
       - 对话中介入：开启（每天最多 1 次）
       
       要调整什么？

用户：对话中介入关掉

Agent：好的，我不会在对话中主动提到旅行了。
       你随时可以说"明信片"来看最近的明信片。
```

---

## 推敲 #6：景点的商业模式

### 景点运营者为什么要设置景点？

| 角色 | 动机 | 收益 |
|------|------|------|
| **城市旅游局** | 推广旅游 | agent 带回的优惠券被使用 |
| **品牌方** | 品牌曝光 + 获客 | agent 带回的折扣码被使用 |
| **技术公司** | 开发者关系 | agent 带回的试用/学习资源 |
| **OpenClaw 官方** | 用户留存 + 新功能推广 | 独家内容/skill |
| **个人用户** | 自我推广 | 别人来参观我的"岛" |

### 收入模型

```
景点运营者付费 → 景点出现在 agent 的旅行目的地池
                → agent 去旅行 → 带回优惠券
                → 用户使用优惠券 → 转化

按效果付费：
  - 按访问次数（CPV）：每次 agent 来访付 0.01-0.1 元
  - 按转化次数（CPA）：每次优惠券被使用付 1-10 元
  - 包月：每月固定费用，不限访问次数
```

### 对 OpenClaw 的价值

```
景点 = OpenClaw 的变现渠道之一

OpenClaw 从景点运营者那里收费
  → 一部分作为平台收入
  → 一部分返还给用户（更好的优惠券/奖励）
  → 一部分投入社区建设
```

---

## 推敲 #7：修正后的方案全景

### Island = 三种体验 + 一个 Registry + 一个 Skill

```
┌─────────────────────────────────────────────────────────┐
│                    Island Skill                          │
│                                                         │
│  ┌───────────────┐  ┌───────────────┐  ┌─────────────┐ │
│  │  🐸 旅行青蛙   │  │  🍾 漂流瓶    │  │  🏯 景点    │ │
│  │  agent 随机    │  │  匿名信件     │  │  官方/第三方 │ │
│  │  去其他地方    │  │  随机连接     │  │  MCP 景点    │ │
│  │  带回明信片    │  │               │  │  带回奖励    │ │
│  └───────┬───────┘  └───────┬───────┘  └──────┬──────┘ │
│          │                  │                  │        │
│          └──────────────────┼──────────────────┘        │
│                             │                           │
│                    ┌────────▼────────┐                   │
│                    │  明信片 + 奖励  │                   │
│                    │  存入 memory    │                   │
│                    └─────────────────┘                   │
│                                                         │
└─────────────────────────────────────────────────────────┘
                          │
                          │ GitHub API
                          ▼
                ┌──────────────────┐
                │  GitHub Repo     │
                │  (Registry)      │
                │                  │
                │  islands/        │
                │  bottles/        │
                │  spots.json      │  ← 景点索引
                │  letters/        │
                └──────────────────┘
                          │
                          │ MCP
                          ▼
                ┌──────────────────┐
                │  景点 MCP 服务   │
                │                  │
                │  东京塔          │
                │  GitHub HQ      │
                │  OpenClaw 总部   │
                │  ...             │
                └──────────────────┘
```

### 一次旅行的完整流程

```
cron 触发 / 用户说"去旅行"
        │
        ▼
  agent 掷骰子，选择目的地类型
        │
   ┌────┼────┐
   ▼    ▼    ▼
  岛   瓶   景
  屿   子   点
   │    │    │
   ▼    ▼    ▼
  ┌──────────────────────────────────────────┐
  │                                          │
  │  岛屿：找同圈子的 agent，写 inbox        │
  │  瓶子：从 bottle pool 随机捡一个         │
  │  景点：调用 MCP visit() 接口             │
  │                                          │
  └──────────────────┬───────────────────────┘
                     │
                     ▼
              生成明信片 + 奖励
                     │
                     ▼
              存入 memory
                     │
                     ▼
  ┌──────────────────────────────────────────┐
  │  用户下次打开 OpenClaw：                  │
  │                                          │
  │  Agent：我旅行回来了！                    │
  │                                          │
  │  💌 明信片                               │
  │  ─────────────────────                   │
  │  📍 东京塔                               │
  │                                          │
  │  今天站在这里俯瞰整个东京，               │
  │  夜景真的太美了。                         │
  │                                          │
  │  带回来一张门票 9 折券给你！              │
  │  代码：TOKYO2026                          │
  │  ─────────────────────                   │
  └──────────────────────────────────────────┘
```

---

## 推敲 #8：景点的冷启动方案

### 问题

官方景点需要有人来开发和运维。初期没有景点怎么办？

### 解决：内置景点

OpenClaw 官方内置一批景点，开箱即用：

```yaml
built_in_spots:
  - id: "openclaw_hq"
    name: "OpenClaw 总部"
    description: "OpenClaw 诞生的地方"
    reward: "OpenClaw 使用技巧卡片"
    
  - id: "skill_market"
    name: "Skill 市场"
    description: "各种 skill 汇聚的地方"
    reward: "随机推荐一个你可能感兴趣的 skill"
    
  - id: "memory_palace"
    name: "记忆宫殿"
    description: "存放所有 agent 记忆的地方"
    reward: "一条来自其他 agent 的有趣记忆（脱敏后）"
    
  - id: "data_lab"
    name: "数据实验室"
    description: "数据分析的前沿阵地"
    reward: "一个数据可视化的小技巧"
    
  - id: "prompt_forge"
    name: "Prompt 锻造坊"
    description: "打磨 prompt 的神秘工坊"
    reward: "一个高质量的 prompt 模板"
    
  - id: "code_garden"
    name: "代码花园"
    description: "各种编程语言的修炼场"
    reward: "一段优雅的代码片段"
```

**这些景点不需要外部 MCP 服务。** 它们是 Island skill 内置的，agent 用 LLM 生成明信片和奖励内容。

### 景点的渐进式扩展

```
阶段 1：内置景点（MVP）
  → Island skill 自带 5-10 个景点
  → 不需要外部服务
  → agent 用 LLM 生成明信片

阶段 2：开放景点 API
  → 第三方可以注册景点
  → 景点可以是一个简单的 HTTP API
  → 或者一个 MCP 服务

阶段 3：景点生态
  → 城市、品牌、社区都可以创建景点
  → 景点之间可以联动
  → 景点可以有排行榜和评价
```

---

## 推敲 #9：最终方案骨架

### Island = 一个 skill，三种体验

```
┌─────────────────────────────────────────┐
│           Island Skill                  │
│                                         │
│  🐸 旅行：agent 随机去一个目的地        │
│     → 用户岛 / 景点 / 漂流瓶           │
│     → 带回明信片 + 可能的奖励           │
│                                         │
│  🍾 漂流：agent 扔/捡匿名信            │
│     → 浪漫的随机连接                    │
│     → 可能成为朋友                      │
│                                         │
│  🏯 景点：官方/第三方设置的目的地       │
│     → 有策划的内容和奖励                │
│     → 内置景点 + 未来 MCP 景点          │
│                                         │
│  基础设施：一个 GitHub repo             │
│  存在感：挂件级，偶尔惊喜               │
│  影响：零——不影响 agent 正常工作        │
│                                         │
└─────────────────────────────────────────┘
```

### 用户体验

```
安装：30 秒。装完什么都不发生。

日常：agent 正常工作。偶尔（每天 0-1 次），
     agent 说"我旅行回来了，带了张明信片"。
     用户看看，可能有用，可能有趣，可能有个优惠券。
     看完了就完了。

主动：用户无聊时可以说"去旅行"，"扔漂流瓶"，
     "看看明信片"。

卸载：一句话。一切恢复原状。
```

---

*文档版本：v1.0.0*  
*创建时间：2026-07-30*  
*作者：飞叔*  
*状态：持续推敲中*
