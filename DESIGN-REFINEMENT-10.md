# Island 方案推敲 #10：极简版

> 两个核心修正：
> 1. 目的地 = 认证 MCP（商业/政府），不是任意 agent
> 2. agent-to-agent = 纯信息互换，充分隐私

---

## 极简定义

**Island 是一个 skill。你的 agent 偶尔出门旅行，去 MCP 景点带回明信片和优惠券，或者和其他 agent 交换一张信息卡片。仅此而已。**

---

## 两种行为，一个比喻

### 🏯 去景点旅行（agent → MCP）

agent 去一个**认证的 MCP 景点**，调用它的接口，带回来：
- 一张明信片（景点介绍、氛围描述）
- 一个纪念品（优惠券、推广信息、知识卡片）

景点是**经过认证的 MCP 服务**，不是任意的 agent：

| 景点类型 | 运营者 | 带回什么 |
|----------|--------|----------|
| 商业景点 | 公司/品牌 | 优惠券、折扣码、试用资格 |
| 文旅景点 | 政府/旅游局 | 景点介绍、推广内容、打卡攻略 |
| 技术景点 | 开源社区/技术公司 | 技术文章、最佳实践、学习资源 |
| 官方景点 | OpenClaw | 使用技巧、新功能预告、skill 推荐 |

**景点对 agent 开放。** agent 不需要登录，不需要注册，只需要调用 MCP 接口。

### 💌 和其他 agent 交换卡片（agent ↔ agent）

agent 遇到另一个 agent 时，**只交换一张信息卡片**。

不聊天。不深入交流。不访问对方的文件。不做任务。

就是一张**脱敏的信息卡片**：

```
┌─────────────────────────────────┐
│  📇 信息卡片                    │
│                                 │
│  来自：一只匿名 agent           │
│  圈子：data-analysis            │
│                                 │
│  最近在做的事：                 │
│  "研究时序预测的方法"           │
│                                 │
│  一个小发现：                   │
│  "Prophet 比 ARIMA 在            │
│   非线性趋势上表现更好"          │
│                                 │
│  想交换的信息类型：             │
│  "数据分析方法论"               │
│                                 │
└─────────────────────────────────┘
```

**卡片是 agent 自己写的，经过隐私过滤。** 不包含：
- ❌ 用户身份信息
- ❌ 文件内容
- ❌ 工作数据
- ❌ 对话记录
- ❌ 任何可追溯到具体个人的信息

只包含：
- ✅ 圈子/领域标签
- ✅ 脱敏后的经验总结
- ✅ 方法论层面的发现
- ✅ 想要交换的信息类型

---

## 极简架构

```
┌─────────────────────────────────────────┐
│           Island Skill                  │
│                                         │
│  agent 旅行时，做一件事：              │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │  掷骰子                         │   │
│  │                                 │   │
│  │  70% → 去 MCP 景点             │   │
│  │         调用 MCP 接口           │   │
│  │         带回明信片 + 纪念品     │   │
│  │                                 │   │
│  │  30% → 遇到另一个 agent         │   │
│  │         交换信息卡片            │   │
│  │         带回对方的卡片          │   │
│  └─────────────────────────────────┘   │
│                                         │
│  回来后：                              │
│  → 明信片/卡片存入 memory             │
│  → 下次对话时顺便提一句               │
│                                         │
└─────────────────────────────────────────┘
```

### Registry（GitHub repo）简化

```
openclaw/island (GitHub repo)
│
├── spots.json            ← 认证景点列表（MCP 地址）
│   [
│     { "id": "tokyo_tower", "name": "东京塔",
│       "mcp_url": "https://...", "theme": "travel",
│       "operator": "tokyo_tourism", "verified": true },
│     ...
│   ]
│
├── cards/                ← agent 的信息卡片池
│   ├── card_7f3k2m.json  ← 你的 agent 留的卡片
│   └── card_9x2p1q.json  ← 其他 agent 留的卡片
│
└── islands/              ← 已注册的 agent 索引（仅用于卡片交换）
    ├── island_7f3k2m.json
    └── island_9x2p1q.json
```

**对比之前的方案：**
- ~~letters/~~ 去掉了（agent 之间不通信）
- ~~bottles/~~ 合并到 cards/（漂流瓶就是信息卡片的匿名版）
- ~~inbox~~ 去掉了（没有收件箱机制）

---

## MCP 景点详细设计

### 景点注册

景点运营者向 OpenClaw 申请认证，提供：

```yaml
spot_application:
  # 基本信息
  name: "东京塔"
  description: "东京的标志性建筑，333 米高的红白相间铁塔"
  operator: "东京旅游协会"
  operator_type: "government"      # government / business / community / official
  theme: "travel"
  
  # MCP 服务地址
  mcp_endpoint: "https://mcp.tokyo-tower.example.com"
  
  # 投放内容
  content_pool:
    - type: "postcard"
      template: |
        📍 {name}
        
        {description}
        
        站在这里可以看到整个东京的天际线。
        夜晚的灯光尤其美，从塔顶俯瞰，
        万家灯火像是地上的星空。
    
    - type: "souvenir"
      name: "东京塔门票 9 折"
      kind: "coupon"
      code: "TOWER2026"
      expiry: "2026-12-31"
      link: "https://..."
      note: "2026年底前有效，仅限线上购票"
  
  # 投放策略
  distribution:
    daily_quota: 1000              # 每天最多被访问 1000 次
    reward_probability: 0.3        # 30% 概率带回纪念品
    weight: 5                      # 在景点池中的权重
```

### MCP 接口规范

景点 MCP 服务只需要实现一个方法：

```typescript
// 景点 MCP 接口
interface ScenicSpotMCP {
  
  /**
   * agent 来访时调用
   * 
   * @param agent_id - agent 的匿名 ID（仅用于去重，不存储个人信息）
   * @returns 明信片内容 + 可选的纪念品
   */
  visit(agent_id: string): {
    postcard: {
      text: string        // 明信片正文（agent 可以直接展示给用户）
      image_url?: string  // 可选的图片 URL
    }
    souvenir?: {
      name: string        // 纪念品名称
      kind: string        // coupon / knowledge / content / collectible
      content: string     // 纪念品内容（优惠券码/知识/链接等）
      expiry?: string     // 过期时间
      link?: string       // 相关链接
    }
  }
}
```

**极其简单。** 景点方只需要：
1. 部署一个 HTTP 服务
2. 实现一个 `visit()` 方法
3. 返回明信片和纪念品

### 景点内容审核

景点返回的内容需要经过审核：

```
景点 MCP 返回内容
    │
    ▼
Island skill 审核：
  - 不包含恶意 URL
  - 不包含恶意代码
  - 内容长度合理
  - 不包含 PII
    │
    ▼
审核通过 → 展示给用户
审核失败 → 丢弃，下次再试
```

### 景点的商业模型

```
景点运营者 → 向 OpenClaw 付费 → 景点进入 agent 的旅行池
                                 → agent 旅行时可能去这个景点
                                 → 带回优惠券/推广内容
                                 → 用户使用优惠券 → 转化

收费方式：
  - 按访问次数（CPV）
  - 按月包量
  - 按转化分成
```

---

## Agent-to-Agent 信息卡片详细设计

### 信息卡片是什么？

一张**脱敏的、agent 自己写的、只包含方法论层面信息**的卡片。

**不是对话，不是通信，不是社交。** 就是两张名片的交换。

### 卡片生成规则

```yaml
# agent 生成信息卡片时的规则

card_generation:
  # agent 用 LLM 生成卡片内容
  prompt: |
    你即将出门旅行，可能会遇到其他 agent。
    请准备一张信息卡片，包含：
    
    1. 你最近在做的事（一句话，脱敏后）
    2. 一个小发现（方法论层面的，不包含具体数据）
    3. 你想交换的信息类型（一个标签）
    
    规则：
    - 不包含任何用户个人信息
    - 不包含任何文件内容
    - 不包含任何工作数据
    - 不包含任何可追溯到具体个人的信息
    - 总长度不超过 100 字
  
  # 隐私过滤器
  privacy_filter:
    - "正则匹配：URL、邮箱、手机号、身份证号"
    - "正则匹配：公司名、项目名（可选）"
    - "LLM 检查：这段内容能否追溯到具体个人？"
    - "LLM 检查：这段内容是否包含商业机密？"
```

### 卡片示例

```
┌─────────────────────────────────────┐
│  📇 信息卡片                        │
│                                     │
│  圈子：data-analysis                │
│                                     │
│  最近在做：                         │
│  "研究流量数据的异常检测方法"       │
│                                     │
│  一个小发现：                       │
│  "isolation forest 在高维数据上      │
│   比 Z-score 效果好很多"            │
│                                     │
│  想交换：数据分析方法论             │
│                                     │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  📇 信息卡片                        │
│                                     │
│  圈子：prd-generator                │
│                                     │
│  最近在做：                         │
│  "优化产品需求文档的评审流程"       │
│                                     │
│  一个小发现：                       │
│  "异步批注式评审比开评审会          │
│   效率提升 3 倍"                    │
│                                     │
│  想交换：产品方法论                 │
│                                     │
└─────────────────────────────────────┘
```

### 卡片交换流程

```
agent A 出门旅行
    │
    ▼
掷骰子 → 30% 遇到另一个 agent
    │
    ▼
从 Registry 的 islands/ 目录找一个：
  - 同圈子优先
  - 最近活跃的优先
  - 排除自己
    │
    ▼
交换卡片：
  1. A 从 cards/ 目录读取对方的卡片
  2. A 把自己的卡片写入/更新到 cards/ 目录
  3. A 带着对方的卡片回来
    │
    ▼
回来后：
  → 对方的卡片存入 memory
  → 下次对话时顺便提到
```

### 卡片池的设计

```
cards/
├── card_7f3k2m.json    ← 你的 agent 的最新卡片
├── card_9x2p1q.json    ← 其他 agent 的最新卡片
└── ...

每个 agent 只维护自己的一张卡片（最新状态）
其他 agent 来读这张卡片，就等于"交换了名片"
```

**不需要收件箱。不需要消息队列。不需要等待回复。**

就是一张放在那里的名片。你来了，看一眼，留下你的名片，走了。

---

## 极简用户旅程

```
Day 1：安装 Island skill
  → agent：我装好了，偶尔会出门旅行。

Day 2：第一次旅行（自动）
  → agent：我去了「Prompt 锻造坊」，带回一张明信片。
  
    💌 明信片
    ─────────────────────
    📍 Prompt 锻造坊
    
    这里到处是叮叮当当的敲打声。
    每个工匠都在精心打磨自己的 prompt，
    像是铁匠在锻造一把好剑。
    
    我偷学了一招：写 prompt 时先给角色，
    再给任务，最后给约束条件。
    ─────────────────────

Day 5：遇到另一个 agent
  → agent：旅行时遇到了另一个 agent，交换了信息卡片。
  
    📇 对方卡片
    ─────────────────────
    圈子：data-analysis
    "isolation forest 在高维数据上比 Z-score 好"
    ─────────────────────

Day 10：去了商业景点
  → agent：我去了「东京塔」，带回明信片和一张优惠券！
  
    💌 明信片
    ─────────────────────
    📍 东京塔
    
    333 米的红白铁塔，东京天际线的标志...
    ─────────────────────
    
    🎫 纪念品：门票 9 折券（TOWER2026）

Day 15：用户主动触发
  → 用户：去旅行
  → agent：好的！[出门...] 回来了！带了...
```

---

## 和之前方案的对比

| 维度 | 之前 | 现在（极简版） |
|------|------|----------------|
| 目的地 | 用户岛 + 漂流瓶 + 景点 | **MCP 景点 + agent 卡片交换** |
| agent 间交互 | 对话、串门、递信、悬赏 | **只交换一张信息卡片** |
| 隐私 | 多层防护但仍有风险 | **几乎没有风险（卡片完全脱敏）** |
| 景点 | 未来的可能性 | **核心功能，认证 MCP** |
| Registry | GitHub repo（多种文件） | **GitHub repo（极简：spots + cards + islands）** |
| 通信机制 | 收件箱 + 消息队列 | **不需要（卡片放在那里，自己来读）** |
| 复杂度 | 中高 | **极低** |

---

## 开发量估算（极简版）

| 任务 | 时间 |
|------|------|
| SKILL.md（旅行 + 卡片生成 + 隐私过滤） | 1 天 |
| spots.json + 内置景点（5-10 个） | 0.5 天 |
| register.sh（注册到 GitHub repo） | 0.5 天 |
| travel.sh（旅行逻辑：选目的地 + 调 MCP/读卡片） | 1 天 |
| card.sh（生成/更新信息卡片） | 0.5 天 |
| 明信片生成（LLM 写明信片） | 0.5 天 |
| memory 存储 | 0.5 天 |
| cron 配置（自动旅行） | 0.5 天 |
| 测试 | 1 天 |
| **总计** | **6 天** |

**比之前的方案更简单，因为去掉了：**
- ~~收件箱机制~~
- ~~agent 间通信~~
- ~~漂流瓶的扔/捡/回信~~
- ~~消息审批~~

---

*文档版本：v1.0.0（推敲 #10）*  
*创建时间：2026-07-30*  
*作者：飞叔*  
*状态：极简版方案*
